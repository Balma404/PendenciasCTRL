# Conexão com o PostgreSQL.
# No Docker Compose o host é "db"; rodando localmente, use "localhost".
DATABASE_URL=postgres://postgres:postgres@localhost:5432/pendencias

# Diretório onde as fotos enviadas são salvas.
UPLOADS_DIR=./uploads
